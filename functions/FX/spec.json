{
  "description": "Foreign Exchange Rates",
  "commands": [
    {
      "id": "convertAmount",
      "syntax": "{{FROM}} {{TO}} {{AMOUNT}}",
      "description": "Find news based on query"
    }
  ]
}
